<p style="color: green"><%= notice %></p>

<%= render @coin %>

<div>
  <%= link_to I18n.t('links.edit'), edit_coin_path(@coin) %> |
  <%= link_to I18n.t('links.list'), coins_path %>

  <%= button_to I18n.t("links.delete"), @coin, method: :delete %>
</div>
